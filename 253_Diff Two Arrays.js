function diffArray(arr1, arr2) {
    var newArr = [];
    console.log(newArr);
    for(i=0; ;i++){

        for (j=0; ;j++){

        }
    }
    // Same, same; but different.
    return newArr;
  }
  
  diffArray([1, 2, 3, 5], [1, 2, 3, 4, 5]);