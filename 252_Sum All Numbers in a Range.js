function sumAll(arr) {
 //  arr.sort(function(a,b) {
 //    return a-b;
 //});
      var min=Math.min(arr[0]);
      var max=Math.max(arr.length);

  for (i=0; i < max;i++){
    console.log(i);

};
   return arr;
 }
//console.log(
sumAll([1, 4]);